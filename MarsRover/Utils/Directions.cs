namespace MarsRover.Utils
{

    // Enumareting directions for the switch-case
    // E = East
    // W = West
    // N = North
    // S = South
    public enum Directions
    {
        E = 1, 
        W = 2,
        N = 3,
        S = 4
    }
}
